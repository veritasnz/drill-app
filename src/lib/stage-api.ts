import drillData from "../../data/drillData";

import Stage from "../models/Stage.model";

// export const getStageByIndex: (stageIndex: number) => Stage = (
//     stageIndex: number
// ) => {
//     return drillData[stageIndex];
// };
